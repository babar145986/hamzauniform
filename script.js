window.addEventListener('load',()=>{document.querySelector('.preloader').classList.add('hide')});
const menu=document.querySelector('.menu-btn'),nav=document.querySelector('.nav');menu.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('show');if(e.target.classList.contains('stats')) startCounters();}}),{threshold:.15});document.querySelectorAll('.reveal,.stats').forEach(el=>io.observe(el));
let counted=false;function startCounters(){if(counted)return;counted=true;document.querySelectorAll('[data-count]').forEach(el=>{let target=+el.dataset.count,n=0,step=Math.max(1,Math.ceil(target/60));let t=setInterval(()=>{n+=step;if(n>=target){n=target;clearInterval(t)}el.textContent=target===100?n+'%':n+'+'},25)})}
const filterBtns=document.querySelectorAll('.filter button'),imgs=document.querySelectorAll('.masonry img');filterBtns.forEach(btn=>btn.onclick=()=>{filterBtns.forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;imgs.forEach(img=>{img.style.display=(f==='all'||img.dataset.cat===f)?'block':'none'})});
const light=document.querySelector('.lightbox'),lightImg=light.querySelector('img');document.querySelectorAll('.masonry img,.product img,.step img').forEach(img=>img.addEventListener('click',()=>{lightImg.src=img.src;light.classList.add('show')}));light.querySelector('button').onclick=()=>light.classList.remove('show');light.addEventListener('click',e=>{if(e.target===light)light.classList.remove('show')});
let last=0;const header=document.querySelector('.header');window.addEventListener('scroll',()=>{const y=scrollY;header.style.transform=y>last&&y>250?'translateY(-105%)':'translateY(0)';last=y});